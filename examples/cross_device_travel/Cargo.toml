[package]
name = "nira-cross-device-travel"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
publish = false

# An isolated handoff example: no changes to the Nira runtime or workspace.
[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
